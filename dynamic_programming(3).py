# 답안 예시
# 결국 다이나믹 프로그래밍은 점화식을 세우는 싸움이구나, 점화식이 a(i) = a(i-1) + a(i-2)*2 임.

n = int(input())
d = [0] * 1001
d[1] = 1
d[2] = 3
for i in range(3, n + 1):
    d[i] = (d[i - 1] + 2 * d[i - 2] % 796796)  # 이 점화식 이해 안되고 있음.

print(d(n))
